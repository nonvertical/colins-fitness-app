// Default exercises are empty — user adds their own via the Exercises page.
// No pre-populated content. All domain content comes from the user.
const defaultExercises = [];

export default defaultExercises;
